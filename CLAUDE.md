# このフォルダについて（Claude Code 向けの指示書）

輸入商品の各院経費を、法人間振替として勘定奉行へ流し込むための月次処理ツールです。
このファイルは、Claude Code がこのフォルダで作業するときの手順書です。

**このフォルダをPC上で開いたセッションでのみ意味を持ちます。**
クラウド（雲マークの付いた）セッションからは Box にアクセスできません。

---

## 「○月分の処理をお願いします」と言われたら

利用者はプログラムに詳しくありません。専門用語を避け、何をしたかを日本語で報告してください。

### 手順

1. **まず試算する。いきなり本番実行しない。**

   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File "scripts\New-YunyuFurikaeCsv.ps1" -DryRun
   ```

   年月の指定がなければ引数なし（PCの日付から今月を自動判定）。
   「10月分」のように言われたら `-Month 202610`、「先月分」なら `-Month -1`。

2. **検算結果を利用者に見せて、判断を仰ぐ。**

   次の点を日本語で要約して伝えること。

   - 処理年月・データ月・計上日・対象法人
   - 行数、医院数、勘定科目ごとの合計
   - ブックの突合表との照合結果（OK か 要確認 か）
   - 明細シート「商品ごと」との照合結果
   - 前月との比較（合計の増減、医院の増減）
   - 警告が出ていれば、その内容と意味

   **警告が1つでも出ていたら、必ず立ち止まって利用者に確認する。**
   特に次の警告は、そのまま進めてはいけない。

   - 「新TWE の値が、明細シートを集計し直した結果と一致しません」
     → ① のブックへの貼り直しが済んでいない可能性。利用者に確認する。
   - 「ブックの合計と食い違う科目があります」
     → ① 側の集計を確認してもらう。
   - 「前回のファイルにも計上日 … の行があります」
     → 同じ月を二重に処理しようとしている可能性。確認する。
   - 「上書き先の候補が N 件あり…」
     → どのファイルに書くか、利用者に選んでもらう。

3. **利用者が了承したら本番実行する。**

   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File "scripts\New-YunyuFurikaeCsv.ps1" -Force
   ```

   `-Force` はスクリプト側の Y/N 確認を省くもの。**利用者の了承を得てからのみ使う。**
   了承なしに `-Force` を付けて実行してはいけない。

4. **完了を報告し、次にやることを伝える。**

   「② を更新しました。このあと勘定奉行へ流し込んでください」まで伝えて終わり。
   奉行への取込は自動化していない（仕訳が確定する操作なので人が行う）。

---

## 絶対に守ること

- **書き込んでよいのは ② の CSV 1ファイルだけ。**
  `輸入振替_樹慶会流し込み用CSV.csv`（法人が変われば法人名の部分が変わる）
- **① の Excel ブックは読むだけ。絶対に変更しない。**
  新TWE への値の書き戻しもしない。
- **Box 内の他のファイルを削除・移動・上書きしない。**
- 試算（`-DryRun`）を見せる前に本番実行しない。

---

## 困ったときの調べ方

パスが見つからない、という報告を受けたとき:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "scripts\Open-KeiriMonth.ps1" -ShowPath
powershell -NoProfile -ExecutionPolicy Bypass -File "scripts\Open-KeiriMonth.ps1" -CsvFolder -ShowPath
```

「想定パス」と「実在する一番深い階層」が両方出るので、どの階層で切れたか分かる。

- 月フォルダ（`2026年10月処理` など）が無いだけなら、まだ作られていないだけ。
- それ以外の階層が無いなら、フォルダ名が変わった可能性。
  `scripts\Common.ps1` の「設定 ここから／ここまで」を直す。

設定を直したら、必ずテストを流して壊していないことを確認する:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "tests\Test-KeiriTools.ps1"
```

モックのフォルダツリーで検証するので、実際の Box には触れない。
`-Workbook "<①のフルパス>"` を付けると CSV 生成まで検証する。

---

## 処理の中身（背景）

```
① C:\Users\<自分>\Box\横浜経理部\内部関係
     \1_日)■■奉行 流し込み、各種作業□□
       \★☆せるふばんく各種振込作業【新宿･横浜】、法人間振替請求書
         \<処理年月>\請求書\根拠ﾃﾞｰﾀ\医療法人\法人間処理データ（ｺﾋﾟｰ）
           \☆入力用輸入商品<データ月>各院経費.xlsm      ← 読むだけ

② C:\Users\<自分>\Box\横浜経理部\内部関係
     \14_バクラク\請求書発行\法人間\法人間_個別流し込みCSV\輸入振替
       \輸入振替_樹慶会流し込み用CSV.csv                 ← ここに書く
```

- **処理年月とデータ月は1か月ずれる。** 2026年09月処理 → 2026年8月のデータ。
- **計上日はデータ月の末日。** 2026年09月処理 → `2026/8/31`。
- 元の手作業: ① の 新TWE シートを B列（法人）で絞り、
  C列→C列 / D列→D列 / F列→G列およびL列 に転記していた。
- ② は Shift_JIS / CRLF / 13列。先頭のデータ行だけ A列が `*`。

詳しい仕様と設定項目は `README.md` を参照。

---

## 用語

| 言葉 | 意味 |
| --- | --- |
| ① | 入力元の Excel（☆入力用輸入商品…各院経費.xlsm） |
| ② | 出力先の CSV（輸入振替_…流し込み用CSV.csv） |
| 新TWE | ① の中の、医院×勘定科目で集計済みのシート |
| 商品ごと | ① の中の、商品単位の明細シート（新TWE の元データ） |
| 突合表 | 新TWE の I〜N 列にある、法人別合計の検算表 |
